---
deployment:
  tasks:
    - export DEPLOYPATH="${DEPLOYPATH:-$HOME/public_html}"
    - /usr/bin/test -f deployment/viejap-release-20260614.zip
    - /bin/mkdir -p "$DEPLOYPATH"
    - /usr/bin/unzip -oq deployment/viejap-release-20260614.zip -d "$DEPLOYPATH"
